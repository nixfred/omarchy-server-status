{
  "name": "omarchy-tailscale-host-monitor",
  "version": "0.6.2",
  "private": true,
  "type": "module",
  "scripts": {
    "status": "bun run backend/server-status.ts status",
    "test": "bun test",
    "check": "bun test && bun build backend/server-status.ts --target=bun --outdir=/tmp/omarchy-server-status-build"
  }
}
